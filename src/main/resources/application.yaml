spring:
  datasource:
    username:
    password:
    url: jdbc:postgresql://localhost:5432/userdemo

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQL9Dialect
